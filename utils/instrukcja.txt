1. instalacja micropythona
	- wejść do katalogu esptool
	- esptool.py --port COM11 erase_flash
	- założyć zworkę i wcisnąć reset
	- esptool.py --port COM11 --baud 115200 write_flash --flash_size=detect 0 binarka_stara.bin
	- wcisnąć reset
	- po wgraniu softu uruchomić terminal, włączyć port na prędkości 115200, wyciągnąć zworkę i wcisnąć reset 
	- powinien uruchomić się micropython
	
2. instalacja webREPL
import webrepl_setup => E => qwerty => y
	
3. podłączenie do sieci
import network
sta = network.WLAN(network.STA_IF)
sta.active(True)
sta.connect("UPC865E232", "RM4yvcnxjurh")
sta.ifconfig()

4. webREPL
- uruchomienie webrepl-master/webrepl.html
- podanie IP z konfiguracji, connect, podanie hasła
- wgrywanie pliku boot.py i main.py
- odłączenie od urządzenia i reset